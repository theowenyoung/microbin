# Require username for HTTP Basic Authentication when
# visiting the service. If basic auth username is set but
# basic auth password is not, just leave the password field
# empty when logging in. You can also just go to
# https://username:password@yourserver.net or
# https://username@yourserver.net if password is not set
# instead of typing into the password
# Default value: unset
# export MICROBIN_BASIC_AUTH_USERNAME=
# export MICROBIN_BASIC_AUTH_PASSWORD=

# Enables administrator interface at yourserver.com/admin/
# Default value: admin
# export MICROBIN_ADMIN_USERNAME=admin
# export MICROBIN_ADMIN_PASSWORD=admin

# Custom footer text. HTML is allowed, e.g. '<', '>' and so on.
# export MICROBIN_FOOTER_TEXT=

# Hides the navigation bar on every page.
# Default value: false
export MICROBIN_HIDE_HEADER=false

# Hides the footer on every page.
# Default value: false
export MICROBIN_HIDE_FOOTER=false

# Hides the MicroBin logo from the navigation bar on every
# page.
# Default value: false
export MICROBIN_HIDE_LOGO=false

# Disables the /pastalist endpoint, essentially making all
# pastas private.
# Default value: false
export MICROBIN_NO_LISTING=false

# Enables syntax highlighting support. When creating a new
# pasta, a new dropdown selector will be added where you can
# select your pasta's syntax, or just leave it empty for no
# highlighting.
export MICROBIN_HIGHLIGHTSYNTAX=true

# Sets the default syntax/display mode selection when creating
# a new pasta. When set to "auto", content type (Markdown, HTML,
# or code) is automatically detected.
# Default value: auto
# Available options: none, auto, md, html, sh, c, cpp, cs, go,
# java, js, py, rs, and other language extensions
export MICROBIN_DEFAULT_SYNTAX=auto

# Enables Markdown rendering. When enabled, pastas with Markdown
# content (auto-detected or manually selected) will be rendered
# as formatted HTML similar to GitHub READMEs.
# Default value: false
export MICROBIN_RENDER_MARKDOWN=true

# Enables HTML rendering in a sandboxed iframe. When enabled,
# pastas with HTML content will be displayed in an iframe.
# SECURITY NOTE: HTML rendering is sandboxed but may still pose
# risks. Only enable if you trust your users or need this feature.
# Default value: false
export MICROBIN_RENDER_HTML=true

# Sets the port for the server will be listening on.
# Default value: 8080
export MICROBIN_PORT=8080

# Sets the bind address for the server will be listening on.
# Both ipv4 and ipv6 are supported. Default value: "0.0.0.0".
export MICROBIN_BIND="0.0.0.0"

# Enables private pastas.
# Default value: true
export MICROBIN_PRIVATE=true

# Disables main CSS styling.
# Default value: false
export MICROBIN_PURE_HTML=false

# Sets the name of the directory where MicroBin creates
# its database and stores attachments.
# Default value: microbin_data
export MICROBIN_DATA_DIR="microbin_data"

# Enables storing pasta data in a JSON file instead of SQLite.
# Default value: false
export MICROBIN_JSON_DB=false

# Add the given public path prefix to all urls.
# Default value: unset
# export MICROBIN_PUBLIC_PATH=

# Sets a shortened path for URLs.
# Default value: unset
# export MICROBIN_SHORT_PATH=

# The password required for uploading, if read-only mode is enabled
# Default value: unset
# export MICROBIN_UPLOADER_PASSWORD=your_password_here

# If set to true, authentication required for uploading
# Default value: false
export MICROBIN_READONLY=false

# Enables showing read count on pasta pages.
# Default value: false
export MICROBIN_SHOW_READ_STATS=true

# Adds your title of choice to the navigation bar.
# Default value: unset
# export MICROBIN_TITLE=

# Number of workers MicroBin is allowed to have.
# Default value: 1
export MICROBIN_THREADS=1

# Sets the garbage collector time limit. Pastas not accessed
# for N days are removed even if they are set to never expire.
# Default value: 90. To turn off GC: 0.
export MICROBIN_GC_DAYS=90

# Enables or disables the "Burn after" function
# Default value: false
export MICROBIN_ENABLE_BURN_AFTER=true

# Sets the default burn after setting on the main screen.
# Default value: 0
export MICROBIN_DEFAULT_BURN_AFTER=0

# Changes the maximum width of the UI from 720 to 1080 pixels.
# Default value: false
export MICROBIN_WIDE=false

# Enables generating QR codes for pastas.
# Default value: false
export MICROBIN_QR=true

# Toggles "Never" expiry settings for pastas.
# Default value: false
export MICROBIN_ETERNAL_PASTA=false

# Enables "Protected" uploads (Read-only).
# Default value: true
export MICROBIN_ENABLE_READONLY=true

# Enables editing functionality on the server.
# Default value: true
export MICROBIN_EDITABLE=false

# Sets the default expiry time setting on the main screen.
# Default value: 24hour
# Available options: 1min, 10min, 1hour, 24hour, 1week, never
export MICROBIN_DEFAULT_EXPIRY=1week

# Disables and hides the file upload option in the UI.
# Default value: false
export MICROBIN_NO_FILE_UPLOAD=false

# Custom CSS URL to replace the built-in stylesheet.
# Default value: unset
# export MICROBIN_CUSTOM_CSS=

# Use short hash strings in URLs instead of animal names.
# Default value: false
export MICROBIN_HASH_IDS=false

# Enables server-side encryption.
# Default value: false
export MICROBIN_ENCRYPTION_CLIENT_SIDE=true

# Enables client-side encryption.
# Default value: false
export MICROBIN_ENCRYPTION_SERVER_SIDE=true

# Limit the maximum file size (in MB) for encrypted uploads.
# Default value: 256
export MICROBIN_MAX_FILE_SIZE_ENCRYPTED_MB=1024

# Limit the maximum file size (in MB) for unencrypted uploads.
# Default value: 2048
export MICROBIN_MAX_FILE_SIZE_UNENCRYPTED_MB=2048

# Disables the feature that checks for available updates.
# Default value: false
export MICROBIN_DISABLE_UPDATE_CHECKING=false

# Disables telemetry if set to true.
# Default value: false
export MICROBIN_DISABLE_TELEMETRY=true

# Enables listing your server in the public MicroBin server list.
# Default value: false
export MICROBIN_LIST_SERVER=false

# Default privacy level for new pastas.
export MICROBIN_DEFAULT_PRIVACY=unlisted

# ============================================
# S3 Storage Configuration (Optional)
# ============================================
# When configured, file attachments will be stored in S3-compatible
# storage (e.g., AWS S3, Backblaze B2, MinIO) instead of local filesystem.
# All four settings (endpoint, bucket, access_key, secret_key) must be
# provided to enable S3 storage.

# S3-compatible endpoint URL
# Example for Backblaze B2: https://s3.us-west-000.backblazeb2.com
# Example for AWS S3: https://s3.amazonaws.com
# export MICROBIN_S3_ENDPOINT=https://s3.us-west-000.backblazeb2.com

# S3 bucket name
# export MICROBIN_S3_BUCKET=your-bucket-name

# S3 access key / key ID
# export MICROBIN_S3_ACCESS_KEY=your-access-key

# S3 secret key
# export MICROBIN_S3_SECRET_KEY=your-secret-key

# S3 region (optional, default: us-west-000)
# export MICROBIN_S3_REGION=us-west-000
